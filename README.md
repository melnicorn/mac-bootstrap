# mac-bootstrap

Bootstrap a fresh macOS machine for development.

## Run

```bash
git clone https://github.com/melnicorn/mac-bootstrap.git
cd mac-bootstrap
chmod +x bootstrap.sh scripts/*.sh
./bootstrap.sh